[tool.poetry]
name = "airosense-worker"
version = "0.1.0"
description = "Worker processes for AiroSense"
authors = ["Your Name <you@example.com>"]
packages = [{ include = "app" }]

[tool.poetry.dependencies]
python = "^3.11"
celery = "5.3.6"
redis = "5.0.4"
pandas = "2.2.2"
pendulum = "3.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "8.3.1"
ruff = "0.5.5"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
